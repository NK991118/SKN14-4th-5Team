# c:/Workspaces/Personal_Anything/3rd_to_4th/fourth_project/.gitignore

# 1. 가상 환경 (Virtual Environment)
# venv 폴더 전체를 무시. 이것만으로도 용량 문제가 해결돼.
venv/
.venv/

# 2. 비밀 정보 (Secrets)
# .env 파일은 절대 올리면 안 돼!
.env

# 3. 데이터베이스 (Database)
# 각자 개발 환경에서 사용하는 데이터베이스 파일은 무시.
db.sqlite3
*.sqlite3

# 4. 파이썬 임시 파일 (Python Cache)
# 파이썬이 실행 속도를 높이기 위해 만드는 임시 파일들.
__pycache__/
*.pyc

# 5. 우리 프로젝트에서 자동 생성되는 파일들
# PDF를 변환해서 만든 이미지들은 원본(PDF)만 있으면 되므로 무시.
static/images/pdf_pages/

# 6. 운영체제 및 에디터 설정 파일 (OS / IDE)
# 각자 다른 개발 환경 설정을 공유하지 않기 위해 무시.
.vscode/
.idea/
.DS_Store